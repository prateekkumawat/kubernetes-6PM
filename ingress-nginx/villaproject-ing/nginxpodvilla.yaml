apiVersion: v1
kind: Pod
metadata:
  name: nginx-app-1
  namespace: nginx-group
  labels:
    application: webapp
    apptype: webserver
    appname: nginx-villa
spec:
  containers:
  - name: nginx-app-1
    image: prateekkumawat/villa:latest
    resources:
      requests: 
        memory: "300Mi"
        cpu: "200m"
      limits:
        memory: "700Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
